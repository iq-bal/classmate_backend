# Backend Server

## Overview
The central backend service for Classmate, managing courses, assignments, and user roles.

## Scripts
- `npm run dev`: Starts the server (port 4000).

## Dependencies
- Express
- GraphQL (Apollo Server + express-graphql)
- Mongoose (MongoDB)
- Multer (File uploads)
