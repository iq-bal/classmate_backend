# Authentication Server

## Overview
The Authentication Server manages user identity and access control for the Classmate Backend ecosystem.

## Scripts
- `npm run dev`: Starts the test server (port 4000).
- `npm run authServer`: Starts the main auth routes server (port 6000).

## Dependencies
- Express
- Mongoose (MongoDB)
- Redis
- JSONWebToken (JWT)
